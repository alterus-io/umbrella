apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gitlab
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://gitlab.com/gitlab-org/charts/gitlab.git
    targetRevision: 4-2-stable
    path: .
    helm:
      values: |-
        global:

          hosts:
            domain: atoy.lol

        certmanager-issuer:
          email: atoy3731@gmail.com
  destination:
    server: https://kubernetes.default.svc
    namespace: gitlab
  syncPolicy:
    automated:
      prune: true